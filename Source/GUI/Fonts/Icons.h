#pragma once

/**
 * Binding files contain I3T specific icon defines used in the app.
 * This allows to easily change icons in the binding file and/or switch binding files
 * entirely without actually changing the icon macros in the application code.
 */
#include "Bindings/BindingFontAwesome.h"
